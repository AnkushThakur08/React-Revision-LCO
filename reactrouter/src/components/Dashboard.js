import React from "react";

const Dashboard = ({state}) => {
    console.log(state)
    return(
        <div>
            <h1>This ${} is of ${} price</h1>
        </div>
    )
}

export default Dashboard;